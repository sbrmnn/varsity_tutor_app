== README

Assuming that you have the latest rails installed along with Ruby.

Instructions:
-------------

Open terminal and type:

Fetch project from github.
```
git clone git@github.com:sbrmnn/varsity_tutor_app.git
```
Run the rails server setting the database password enviornmental variable.
```
DB_PASSWORD=<your db password> rails s
```
